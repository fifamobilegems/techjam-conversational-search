__pycache__/
*.py[cod]
.DS_Store
.env
# Any local variant (.env.bak, .env.local, ...). Does NOT match .env_example,
# which is the tracked template and carries no real values.
.env.*
results.json
logs/
data/catalog.jsonl
# Dense-retrieval build products. Rebuild with:
#   python -m scripts.build_embeddings
data/embeddings/
.venv/
venv/
data/releases/
# Organizer-only code, private evaluation data, manifests, and build reports.
organizer/
secure/
# Internal provenance and release-audit working documents.
docs/audits/
docs/data_selection_audit.md
docs/participant_release_checklist.md
# Organizer-only pipeline tests.
tests/test_5core_builder.py
tests/test_organizer_pipeline.py
*.log
debug/
results_*.json
# Raw ESCI release (~1.2 GB) cached by scripts/build_esci_gold.py. The derived
# join, data/esci_gold_relevance.jsonl, is committed; the source is not.
data/esci_raw/
# Reranker training tensors. Rebuild in ~1 min with:
#   python -m scripts.build_rerank_features
data/rerank_features.npz
